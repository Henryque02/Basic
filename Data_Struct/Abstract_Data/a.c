#include<stdio.h>

int main(){
    int base;

    printf("Insira o tamanho da base: ");
    scanf("%d", &base);
    
    for (int i = 0; i < base; i++)
    {
        printf("\n")
        for (int j = 0; j < base; j++)
        {
            d
        }
        
    }
    
    
    return 0;
}